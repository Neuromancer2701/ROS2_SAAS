# Empty __init__.py to make this directory a Python package.
# This allows `ros2 run tb3_simulation_pkg teleop_node_py` to find the module
# if teleop_node.py is correctly placed and setup.py entry_points are configured.

# You can optionally import modules here if you want them to be available
# when someone imports `tb3_simulation_pkg`. For example:
# from .teleop_node import TeleopNodePy

# For now, keeping it empty is standard for simple packages.
